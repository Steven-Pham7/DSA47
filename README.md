Once you clone the reposotory make sure to install the packages used.
packages:
pandas
sortedcontainers
streamlit
Then open the terminal and run this line:
streamlit run ./main.py(or filepath to the main file in this repository)
